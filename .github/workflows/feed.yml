name: Update this repo's README

on:
  schedule:
    # Once a day at 8 AM
    - cron: 30 17 * * *

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: JasonEtco/rss-to-readme@v1
        with:
          feed-url: https://otherm.cc/rss.xml
          readme-section: feed
