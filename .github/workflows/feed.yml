name: Update this repo's README

on:
  schedule:
    - cron: * 17 * * *

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: JasonEtco/rss-to-readme@v1
        with:
          feed-url: https://0066cc.com/rss.xml
          readme-section: feed
